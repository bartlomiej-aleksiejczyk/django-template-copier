# questions
project_name:
  type: str
  help: What is the new project name (it will impact dir name, kebab case is suggested)?
choosen_databse:
  type: str
  help: Which database do you want to use?
  choices:
    PostgreSQL: postgres
    SQLite: sqlite
  default: sqlite
is_huey_enabled:
  type: bool
  help: Do you want to use the Huey to manage asynchrnous tasks?
  default: no

_subdirectory: template
